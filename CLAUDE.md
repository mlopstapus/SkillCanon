# SkillCanon

Self-hosted skill/prompt registry distributed via REST, MCP, and a CLI, with hierarchical team/policy/objective governance. A unified Next.js/TypeScript application (pnpm-managed, `src/`) at the repository root per `docs/context/repo-structure.md`, plus a standalone `cli/` package. Postgres, deployed via Docker Compose (local self-host) — the Helm chart (`charts/spechub/`) still describes an older split backend/frontend deployment shape and has not yet been reworked for the unified app (don't use it to deploy the current app as-is). The previous split Python/FastAPI + Next.js 14 implementation (`legacy/backend/`, `legacy/frontend/`) was fully ported over epic by epic and deleted 2026-08-12 once every epic that needed it as a reference was done — if you see a `legacy/` reference elsewhere in this file, it's describing history from before that deletion, not current repo state.

## Key commands

| Command | Run |
|---------|-----|
| Install | `pnpm install` |
| Dev | `pnpm dev` |
| Build | `pnpm build` |
| Lint | `pnpm lint` |
| Type check | `pnpm typecheck` |
| Test | `pnpm test` |
| Rebuild (self-hosted stack) | `docker compose up -d` |

## Notes

- In managed Multica workspaces where `pnpm` is not installed globally, use `corepack pnpm <script>` for the same project commands rather than switching package managers.
- If unrelated Multica-managed containers occupy SkillCanon default ports and cannot be stopped, health-check the app with a temporary Compose file/project that remaps ports (for example app 3001 and Postgres 5434); leave `docker-compose.yaml` defaults unchanged.
- `skillcanon-app-1`/`skillcanon-database-1` in a Multica workspace are typically a long-lived shared dev stack (observed up 47h+ across sessions), alongside the platform's own `multica-*`/`infra-controller-1` containers on the same Docker daemon. Don't run `docker compose down`/rebuild for a change that doesn't touch `docker-compose.yaml`/`Dockerfile`/runtime app code — it's disruptive to concurrent work for no verification benefit; the Testcontainers-backed `pnpm test` suite already proves migrations/queries work against a real Postgres in isolation.
- `docker compose up -d` builds and runs the unified app (`app` service, from the root `Dockerfile`) plus Postgres (`database` service) — see epic `001-typescript-refactor-foundation`'s `005-docker-compose-dev-environment`. The database container has no pre-initialized schema — `pnpm db:migrate` (Drizzle) owns schema creation from an empty database.
- Compliance scope: SOC2, plus NIST alignment (no dedicated automated check yet — review access control, audit logging, and encryption manually).
- ESLint config uses the new flat-config format (`eslint.config.mjs`) importing `eslint-config-next`'s default export directly — **do not** use the `FlatCompat` shim with the legacy `"next/core-web-vitals"`/`"next/typescript"` string names; `eslint-config-next@16` already ships a native flat array and wrapping it in `FlatCompat` causes a circular-JSON crash.
- Pin `eslint@^9` and `typescript@^5` explicitly in the new scaffold, not `"latest"` — `eslint-config-next`'s `typescript-eslint`/plugin peer deps don't yet support `eslint@10`/`typescript@7`.
- When relocating a top-level directory in this repo (as happened moving `backend/`→`legacy/backend/`, `frontend/`→`legacy/frontend/`), check `docker-compose.yaml` build paths, `.gitignore`/`.dockerignore`, and `CLAUDE.md`/`.claude/anchorstack/project.md`/`README.md` for stale references — none of these update themselves.
- `drizzle-kit`'s `pushSchema()` (from `drizzle-kit/api`) calls `process.exit()` internally when stdout/stdin isn't a TTY — it's built for the interactive `drizzle-kit push` CLI, not for programmatic use inside a test runner (kills the vitest worker outright). For Testcontainers-backed schema/column tests, use the non-interactive pair `generateDrizzleJson(imports, prevId, schemaFilters)` + `generateMigration(prev, cur)` (also from `drizzle-kit/api`) to get plain SQL statements, then execute them directly — see `src/shared/db/columns.test.ts`.
- RLS session context is set via `select set_config('app.current_org_id', $1, true)`, never a literal `SET LOCAL ... = $1` — Postgres's `SET` command doesn't accept bind parameters. See `docs/context/database-conventions.md` and `src/shared/db/tenant-context.ts`.
- Completed backlog items get moved into `backlog/<epic>/archive/` (not just `status: done` flipped in place) — matches the convention already used for `backlog/000-foundations/archive/`. If an item's own Acceptance Criteria aren't all met yet (e.g. one is blocked on a separate not-yet-built backlog item), leave `status: open` and don't archive it — check off only what's actually true, don't force-complete.
- `eslint-plugin-boundaries` (added in epic 001's `003-module-boundary-lint-enforcement`) has real, non-obvious v7 gotchas — verify against the installed version's actual behavior (not just docs) before trusting a config:
  - `boundaries/entry-point` and `boundaries/element-types` are **deprecated** aliases in v7; use the canonical `boundaries/dependencies` rule with a `policies` array instead.
  - Element patterns must **not** include a trailing `**` (e.g. `src/bcs/*`, not `src/bcs/*/**`) — `partialMatch` (default `true`) already expands folder patterns internally, and an explicit `**` double-appends a wildcard, shifting the matched "element path" down into each subdirectory and breaking same-context (`dependency.relationship.to === "internal"`) detection.
  - The rule silently skips analyzing imports from any file that is *both* `element.isUnknown` and `file.isUnknown` — every top-level source directory that should be enforced (e.g. `src/app/`) needs its own `boundaries/elements` entry, even if no policy ever references it directly by type.
  - Import target resolution is real filesystem resolution (`eslint-import-resolver-node`); a nonexistent/purely-virtual import target is silently unclassified and produces zero diagnostics. Fixture tests need a real (even if transient, written-then-deleted) target file on disk — only the *importing* file's content can be pure in-memory text via `ESLint.lintText()`'s `filePath` option.
  - `ESLINT_PLUGIN_BOUNDARIES_DEBUG=1` prints the plugin's exact entity/dependency classification per import — the fastest way to debug an unexpected pass/fail.
- In Multica planner→builder handoffs, the planner may have created Speckit artifacts in its own prior task workdir rather than the builder task workdir; inspect `multica issue runs <issue-id> --output json` for the planner run `work_dir` and continue in that checkout/branch before concluding artifacts are missing.
- The issue's `speckit_branch` metadata value (e.g. `023-workflow-model-crud`) is the **feature name**, not necessarily a literal git branch that exists anywhere — a planner's actual git branch is its own Multica run-scoped name (`agent/planning-agent/<task-id>`), which committed the spec under `specs/<speckit_branch>/`. `git branch -a` for the literal `speckit_branch` string first; if it's absent, search for a commit message referencing that feature name (`git log --all --oneline | grep <feature-name>`) and use the branch/commit that has it — don't stop and report "missing" just because the exact string isn't a ref. Separately: that planner branch may already be checked out in a *different* Multica task's git worktree on the same machine (`git checkout <branch>` fails with "already used by worktree") — in that case `git checkout -b <any-local-name> <that-commit-sha>` onto a differently-named local branch pointing at the same commit works fine and isn't "creating a separate implementation branch" in the sense the Builder Agent workflow warns against (the history and content are identical, only the local ref name differs).
- Speckit's `/speckit-specify` does not create a git branch in this repo (no `hooks.before_specify` registered in `.specify/extensions.yml`) — implementation work from `/speckit-implement` lands directly on whatever branch is checked out. Create the feature branch manually before `/speckit-implement` (or at latest before `as-finish`/`as-sync`) rather than letting substantial work accumulate uncommitted on `main`.
- `as-finish`'s Docker-rebuild step is no longer a no-op as of `005-docker-compose-dev-environment`: `docker compose up -d` now builds/runs the new scaffold (`app`) and Postgres (`database`), not the legacy stack — run it whenever a branch's diff touches `Dockerfile`, `database/`, or `docker-compose.yaml`.
- The repo now has **two** Dockerfiles — `legacy/backend/Dockerfile` and `legacy/frontend/Dockerfile` were removed in `005-docker-compose-dev-environment` once the root one covered both: root `Dockerfile` (new scaffold, added in `004-ci-pipeline`, built via `docker build .` from repo root, and now also what `docker-compose.yaml`'s `app` service builds) and `database/Dockerfile` (unchanged, used by both `docker-compose.yaml`'s `database` service and CI). The root one is also what `.github/workflows/ci.yml`'s `docker-build` job and `docker-publish.yml` build/push.
- The new scaffold has no `public/` directory yet (no static assets). The root `Dockerfile`'s runtime stage needs one for `COPY --from=build /app/public ./public` to succeed alongside `output: "standalone"` — the build stage runs `mkdir -p public` before `pnpm build` rather than assuming the folder exists. Remove that workaround once a real `public/` directory lands.
- CI/CD (epic 001's `004-ci-pipeline`) established the registry convention: the app image publishes to `ghcr.io/mlopstapus/skillcanon` (build-only on every PR via `ci.yml`'s `docker-build` job, push only on merge to `main` via the separate `docker-publish.yml`) — same registry/org as the existing Helm chart publish (`ghcr.io/mlopstapus/charts`). AWS SaaS pulling from ECR instead (per `docs/context/deployment.md`) is an explicitly deferred, unresolved decision for a future deployment-epic item, not something `004-ci-pipeline` wired up.
- `main`'s required branch-protection check is (or will be) the single aggregate job `ci-gate` in `.github/workflows/ci.yml` (`needs:` every constituent job) — per `specs/004-ci-pipeline/contracts/required-check.md`, always add new CI jobs to `ci-gate`'s `needs:` list rather than registering a second required check in GitHub's branch protection settings.
- Testcontainers-backed tests (`src/shared/db/*.test.ts`) self-provision their own ephemeral Postgres container per test file via `startTestDb()` — a CI `test` job needs nothing beyond the runner having Docker available (true by default on GitHub's `ubuntu-latest`); don't add a redundant `services: postgres:` block to a workflow file, it would duplicate isolation Testcontainers already provides.
- The root `Dockerfile`'s runtime stage is Next.js's `.next/standalone` output, which only bundles the dependencies actually traced from the app's own import graph — it does **not** include the full `node_modules` (confirmed: `docker compose exec app ls node_modules` shows only `next`/`react`/`react-dom` today). A package like `postgres` or `drizzle-orm` that no route/component imports will be missing at runtime even though it's a real `dependencies` entry in `package.json` — `require('postgres')` inside the running container throws `MODULE_NOT_FOUND`. For an ad hoc connectivity check from inside the container, use a built-in module instead (e.g. Node's `net.createConnection`), not a driver package that isn't actually bundled.
- `docker-compose.yaml`'s Postgres credentials (`POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB`, and the `DATABASE_URL`/`MIGRATION_DATABASE_URL` built from them) use `${VAR:-skillcanon}`-style Compose interpolation, not a bare literal — this file is also the self-host deployment mechanism (see the "Rebuild (self-hosted stack)" row above), so a self-host operator can set a real credential via a shell env var or a `.env` next to the compose file with zero file edit, while local dev keeps the zero-config `skillcanon`/`skillcanon` default. Keep this pattern for any future compose-level credential; a bare hardcoded literal in this specific file is a real (if modest) security finding, not just a style nit, since it ships in a file some deployments run as-is.
- When a backlog item is archived (moved into `backlog/<epic>/archive/`), also update the epic's own `EPIC.md` Features-list link to point at `archive/<file>`, not just tick the checkbox — items `001`–`004` in `backlog/001-typescript-refactor-foundation/EPIC.md` had this exact bug (checked off but still linking to the pre-archive path) for multiple prior sessions before it was caught and fixed in `005-docker-compose-dev-environment`.
- `vitest.config.ts` needs its own `resolve.alias` mirroring `tsconfig.json`'s `paths` (`@/bcs/*`, `@/shared/*`, `@/*`) — Next.js resolves those natively via webpack/Turbopack, but Vitest is a separate (Vite) bundler that does not pick up tsconfig `paths` on its own; a test importing `@/shared/...` fails with "Cannot find package" until the alias is added.
- `pnpm db:generate` needs `MIGRATION_DATABASE_URL` set to a real-looking (non-`REPLACE_ME`) value even when no live DB is reachable — `drizzle.config.ts` evaluates `getConnectionString()` eagerly to build `dbCredentials.url`, before drizzle-kit's actual (connectionless) schema diffing runs. For local one-off generation: `MIGRATION_DATABASE_URL="postgresql://x:x@localhost:5432/skillcanon" pnpm db:generate`.
- `drizzle-kit generate` names new migrations with a random adjective-noun tag (e.g. `0001_striped_phantom_reporter`) — always rename the `.sql` file **and** update its `tag` in `drizzle/migrations/meta/_journal.json` to match this repo's `<timestamp>_<bc-name>_<short-description>` convention (`docs/context/database-conventions.md`) before committing; the numbered `meta/000N_snapshot.json` files don't need renaming, only the journal's `tag` string and the `.sql` filename.
- `pnpm test -- <path>` does **not** scope the Vitest run to `<path>` — the `--` passthrough gets swallowed and the whole suite runs anyway. To scope a run, invoke Vitest directly: `pnpm vitest run <path>`.
- Full `pnpm test` currently covers 66 files / 314 tests, mostly Testcontainers Postgres spin-up — run it in the background or with an extended timeout rather than a short foreground timeout.
- Next.js 16.2.11 currently resolves PostCSS 8.4.x, which fails the high-severity advisory audit; keep the root pnpm override at `postcss >=8.5.22` and regenerate the lockfile when upgrading Next or PostCSS.
- drizzle-orm's postgres-js driver wraps the real Postgres error (the one carrying the SQLSTATE `.code`, e.g. `23505` for a unique violation) inside `.cause` on a thrown `DrizzleQueryError` — code that maps DB errors to domain errors (e.g. a friendly "already exists" message) must check `err.cause?.code`, not just `err.code`, or the mapping silently never matches.
- When adding a Postgres advisory lock to guard a *mode-conditional* invariant (e.g. a self-hosted-only single-row guard), scope the lock acquisition to that mode too — an unconditional lock serializes every caller globally (including callers in the other mode, e.g. SaaS, that have no invariant to protect and would otherwise create many rows concurrently without issue).
- Any test file that creates more than one `identity_access.organizations` row (e.g. to test org-scoped behavior in another BC feature like Team Hierarchy) must `vi.stubEnv("STRIPE_ENABLED", "true")` in a `beforeEach`/`afterEach` pair — `createOrganization`'s self-hosted single-org guard defaults on and rejects the second `createOrganization` call otherwise. Don't re-test the guard itself in these files (already covered by `create-organization.test.ts`); just stub past it.
- A Drizzle repo function that takes `organizationId` as a parameter but doesn't actually use it in the query's `where` clause is a real cross-tenant bug, not just unused-parameter noise — caught once already (`teams-repo.ts`'s `findByParent` accepted `organizationId` but only filtered by `parentTeamId`, so a root-level listing query would have returned every organization's root teams). Audit every new repo function for this specifically; TypeScript won't catch it since the parameter is still "used" as a type, just not in the actual filter.
- A self-referential Drizzle FK (e.g. `teams.parent_team_id` pointing back at `teams.id`) needs an explicit `(): AnyPgColumn => teams.id` arrow-function reference (from `drizzle-orm/pg-core`), not a direct `teams.id` reference — the table constant isn't fully defined yet at the point the column builder runs, so a direct reference is a temporal-dead-zone error; the lazy arrow function defers evaluation until the table is complete.
- Postgres advisory locks scoped to something *other* than a single global invariant (e.g. per-organization, not per-install) should hash the scoping value rather than lock on a single shared constant — `pg_advisory_xact_lock(hashtext('<namespace>'), hashtext(<scopeValue>))`'s two-int32 form combines a fixed namespace (avoiding collisions with unrelated advisory locks elsewhere in the codebase) with a per-scope hash, so only operations sharing that scope value actually serialize against each other.
- Test fixtures across different `it()` blocks in the same file need unique slugs/identifiers (e.g. `` `backend-${randomUUID()}` ``, not a bare literal) whenever the assertion queries by that value without also scoping by organization/parent — otherwise a later test's assertion can silently match a same-named row a different, unrelated test already created. Caught twice in this repo's history (see `create-organization.test.ts`'s slug tests, and `insert-team-between.test.ts`'s original `"backend"` literal).
- Password hashing uses the `bcryptjs` npm package (pure JS), not native `bcrypt` — avoids the native-binding/`node-gyp` build risk on top of the `.next/standalone` bundling gotcha already noted above. `bcryptjs` produces standard, interoperable bcrypt hashes (same `$2a$`/`$2b$` format as the legacy Python `bcrypt` library) with zero native-compilation risk. Reach for `bcryptjs` in any future feature needing bcrypt hashing (password reset, invitations, etc.), not `bcrypt`.
- When a column's FK target table is created by a *later* feature (this repo's established pattern — e.g. `teams.owner_id` deferred no-FK until `identity_access.users` existed, added by `007-user-accounts-registration`), completing that FK afterward will break any existing test that used a fake/random UUID for that column (`update-team.test.ts`'s `newOwnerId = randomUUID()` did exactly this) — fix the fixture to create and use a real referenced row, not just the FK migration.
- The self-referential-FK lazy-arrow pattern (`(): AnyPgColumn => teams.id`, noted above) also applies to genuinely circular cross-table FKs, not just self-references — `teams.owner_id` references `users.id` while `users.team_id` references `teams.id`; whichever table is declared first in `schema.ts` needs the lazy arrow for its forward reference to the not-yet-defined table, and at least one side of the pair must be nullable (`owner_id` is) so the two rows can be inserted in either order and linked afterward.

- `identity_access.users.email`/`username` are unique per `(organization_id, email/username)` only, **not globally** (PDR-003) — a login-style flow that needs to resolve a user *from* an email, with no organization context yet (the user hasn't been identified), cannot filter by `organization_id` at all. Query across every organization instead (`users-repo.findByEmail`, no org filter) and resolve the organization from whichever candidate's password actually matches — see `008-jwt-session-auth`'s research.md §8 for the full reasoning and the failed-login audit-attribution edge cases (zero matches vs. exactly one vs. more than one same-email account across orgs).
- Despite `docs/context/database-conventions.md`'s "FK to `identity_access.organizations.id`" framing, **no `organization_id` column anywhere in this codebase actually carries a Drizzle `.references()` FK** — not `teams.organization_id`, not `users.organization_id`, confirmed by grepping the generated migration SQL for `REFERENCES`. Match this real (not aspirational-doc) precedent for any new `organization_id` column rather than being the first to add an FK no sibling table has.
- `vi.useFakeTimers()` does not mix reliably with Testcontainers-backed DB tests in this repo — combining them in the same `it()` caused a test timeout that left fake-timer state bleeding into the *next* test in the file too (both failed, not just the one using fake timers). For testing JWT/session expiry against a real DB-backed flow, mock the lower-level verify function (e.g. `vi.spyOn(jwtModule, "verifySessionJwt").mockResolvedValueOnce(null)`) instead of advancing a fake clock; reserve `vi.useFakeTimers()` for pure-function unit tests with no DB I/O (e.g. `jwt.test.ts` itself, which has no Testcontainers dependency and works fine).
- Cross-BC test assertions (e.g. identity-access's tests verifying audit-compliance wrote a row) must not import another BC's internal `application/*` or `infrastructure/*` modules directly — `eslint-plugin-boundaries` blocks it even from a `.test.ts` file. Two ways around it that stay compliant: (1) `vi.spyOn` through the target BC's public barrel (`import * as auditCompliance from "@/bcs/audit-compliance"`, then `vi.spyOn(auditCompliance, "record")`) — ES live bindings mean this still intercepts the call even though the barrel just re-exports; (2) for reading rows back, use raw SQL via `sql\`select ... from audit.audit_events where ...\`` + `db.execute()` instead of importing the schema table object.
- Identity-access epic features consistently build only the `src/bcs/identity-access/{domain,application,infrastructure}` layer, never an actual `src/app/**` route or page — even when the originating backlog item's wording says "endpoint" (`004-jwt-session-auth`'s "Login endpoint" bullet was satisfied by an application-layer `login()` function, not a route). Both `007-user-accounts-registration` and `008-jwt-session-auth` made this same call, each documented in their own research.md — actual HTTP wiring and the `(auth)` UI pages stay owned by `008-distribution`, which explicitly depends on epic 002 finishing first. Don't build a route/page inside an epic-002 feature; it duplicates work Distribution's epic already owns and gets built before that epic's error-mapping/session-auth conventions exist in code.
- A locally-running `docker compose` Postgres volume (`skillcanon_pgdata` / `spechub_pgdata`, whichever name predates the current `docker-compose.yaml`) only runs `initdb` once, at volume creation — if the Compose file's `POSTGRES_USER`/`POSTGRES_DB` defaults change later (e.g. the SpecHub→SkillCanon rename changed them from `spechub` to `skillcanon`), an existing volume keeps the *old* role/database name forever, and `pnpm db:migrate`/any connection using the new default fails with `role "skillcanon" does not exist`. Fix by recreating the volume (`docker compose down -v`, local dev data only, safe to drop) rather than debugging it as an app/migration bug.
- It's an established, precedented move in this repo for a feature in one epic to pull forward and complete *part* of a not-yet-started future epic's backlog item, when it has a real, immediate need the future epic would otherwise gate on (see `008-jwt-session-auth` completing `003-audit-compliance/001`'s schema/`record()`/redaction/index requirements early for login/logout audit events). Document it thoroughly on both sides: the pulling feature's plan.md Complexity Tracking, and a note on the future epic's own backlog item/EPIC.md explaining what's done vs. still open — leave that item's `status: open` and don't check off requirements it didn't actually deliver (e.g. a "retrofit existing mutations" bullet stays unchecked if nothing retrofitted anything).

- `shared/db/with-audit.ts`'s `withAudit(db, mutationFn, auditWriteFn)` does **not** pass `mutationFn`'s return value into `auditWriteFn` — both closures only share the same `tx`. This is fine for an UPDATE/DELETE audit event (the id is already known), but a CREATE operation whose audit event needs the new row's id has no way to get it from `auditWriteFn`. Fix: generate the id client-side (`randomUUID()`) *before* calling `withAudit`, and pass it into both closures — as an explicit `id` field on the insert and as the audit event's `resourceId`/`actorUserId`. `009-invitations` added this pattern (an optional `id?: string` passthrough on `invitations-repo.insert`, `users-repo.insert`, and `insertValidatedUser`'s params — all backward-compatible, existing callers omit it and the DB default still applies) as the first real (non-test) caller of `withAudit()`. Reuse this same client-generated-id pattern for any future CREATE+audit pairing through `withAudit`.
- A hard FK on a child table (e.g. `invitations.team_id → teams.id`, `invitations.invited_by_id → users.id`) means Postgres refuses to delete the parent row while **any** child row still references it, in any lifecycle state — not just "active" ones. Before writing a test for a "the referenced row was deleted out from under me" edge case, check whether the referencing column actually has a hard FK; if so (this codebase's default posture — hard-delete-everywhere, no `ON DELETE CASCADE`/`SET NULL` — per `docs/context/database-conventions.md`), the scenario is likely unreachable, and the spec/data-model should document the *stronger* guarantee instead of describing a runtime check that can never fire (`009-invitations`'s original "team deleted before acceptance" edge case turned out to be exactly this — discovered only when the test setup itself failed with a `23503` foreign-key-violation before the actual assertion ran).
- `src/shared/logging` (a `pino` instance via `createLogger(env?)`/`logger`) and `src/shared/email` (`sendEmail({ to, subject, text })`, SMTP via `nodemailer` when `SMTP_HOST` is configured, else logs and returns) went from empty/nonexistent to real, first implementations in `009-invitations` — reuse these rather than reaching for `console.log` or a new ad hoc email path in future features. SES (the managed-SaaS email path from `docs/context/third-party-services.md`) is still **not** implemented — no SaaS/AWS-credentialed deployment surface exists yet to build or verify it against; tracked in that document's own "Implementation status" section (not a backlog item, since the originating foundations item is already archived).
- `src/bcs/<name>/{CONTRACT.md,OWNERSHIP.md}` are the architecture-governance docs from the `as-architect` design phase (public API surface, data contracts, events, folder/DB ownership — no code), living alongside the actual TypeScript implementation (`{domain,application,infrastructure}` + an `index.ts` barrel) in the same bounded-context folder — moved there from a separate top-level `bcs/<name>/` in 2026-07-23 since keeping contract and implementation side by side made more sense than a parallel directory. Per `docs/context/repo-structure.md`, `src/bcs/<name>/index.ts` should export exactly what that context's `CONTRACT.md` "Exposed APIs" table lists — when a feature adds a new exposed function, update both in the same change (e.g. filling in a function's real signature once implemented, as `010-api-keys` did for `authenticateApiKey`/`createApiKey`/`revokeApiKey`, which `CONTRACT.md` had already listed as forward-looking entries).
- Pure domain-layer types/functions (e.g. `domain/invitation.ts`'s `deriveInvitationState`, `domain/api-key.ts`'s `isValidScopeShape`/`isScopeAllowedForRole`) do **not** get their own standalone `.test.ts` file in this codebase — no `domain/*.test.ts` file exists anywhere in `identity-access`. They're exercised indirectly through whichever application-layer tests call them (e.g. `deriveInvitationState` via `revoke-invitation.test.ts`/`list-invitations.test.ts`). Follow this precedent for new domain-layer pure functions rather than introducing a new standalone-domain-test-file convention.
- `010-api-keys`'s `isScopeAllowedForRole` caps a key's requested scopes against the coarse `admin`/`member` role only (`member`: `:read` scopes only; `admin`: any well-formed scope) — a deliberate stopgap, not a real per-resource permission system, adopted because no bounded context beyond `identity-access` exists yet to define real resource-level permissions against. Any future fine-grained RBAC/permission feature should explicitly reconcile with this rule (e.g. replace it, or re-derive it from the new system) rather than leaving it as an orphaned, stale check.

- `011-tenant-isolation-rls` enabled Postgres RLS on all five `identity_access` tables, but a naive rollout would have broken every flow that must resolve an identity with **no organization context yet**: `login` (email lookup), `authenticateSession`/`logout` (JWT subject → `getUser`), `authenticateApiKey` (hash lookup), `acceptInvitation` (token lookup), and org/team/admin bootstrap. Fixed with a second role, `skillcanon_auth` (`SELECT, INSERT, UPDATE`, no `DELETE`, permissive `USING (true)` policy scoped `TO skillcanon_auth` on all 5 tables, coexisting with `skillcanon_app`'s restrictive policy on the same tables since each is scoped `TO <role>` explicitly, never left as default `PUBLIC`). New `shared/db/client.ts` export `authDb`; new `TestDb.authDb` in `shared/db/test-helpers.ts`. `bcs/identity-access/CONTRACT.md` documents exactly which functions require it — any future route/MCP handler calling one of them (via `008-distribution`) must use `authDb`, not the ordinary `db` (tracked in `backlog/002-identity-access/008-authdb-consumer-handoff.md`).
- A Postgres role granted only `INSERT` on a table still fails on any `INSERT ... RETURNING` — Postgres requires `SELECT` privilege on whatever columns are returned. Bit `skillcanon_auth` specifically because `audit-compliance`'s `record()`/`insert()` uses `.returning()`; the fix was granting `SELECT, INSERT` (not `INSERT`-only) on `audit.audit_events` for that role. Remember this for any future narrowly-scoped role whose write path calls into code using `.returning()`.
- `docker-compose.yaml`'s `app` service's `DATABASE_URL` was identical to `MIGRATION_DATABASE_URL` since `005-docker-compose-dev-environment` — both pointing at the Postgres superuser — meaning the self-hosted app ran as the schema owner and bypassed RLS entirely. Fixed in `011-tenant-isolation-rls`: `DATABASE_URL` now uses `skillcanon_app`, with a new `AUTH_DATABASE_URL`/`skillcanon_auth` alongside it; `MIGRATION_DATABASE_URL` alone still uses the superuser. If RLS is ever added to a table again without checking this, verify by connecting to the running `database` container as `skillcanon_app` and confirming an unscoped query throws `unrecognized configuration parameter "app.current_org_id"`.
- Once RLS is live on a table, a function with two separate error branches for "not found" vs. "found, but wrong org" (e.g. `reparentTeam`'s `CrossOrgReparentError`) will only ever hit the "not found" branch when called through a properly org-scoped connection — RLS makes the other org's row invisible before the app-layer comparison runs. This is consistent with this codebase's own M3/FR-008 convention (cross-org denial is always indistinguishable from not-found) and not a regression; update the test's assertion to a generic `.rejects.toThrow()` rather than trying to preserve the now-unreachable specific error class.
- When changing a function's signature, grep the **import path**, not just the call-site name, to find real callers — a local import alias can make two completely different functions share a call-site name. `provision-team-and-admin.ts` does `import { update as updateTeam } from "../infrastructure/teams-repo"`, which looks identical by name to `application/update-team.ts`'s own `updateTeam` but is a different function entirely; a plain `grep "updateTeam("` flagged it as a caller of the latter when it wasn't.
- Whenever an audit/implementation surfaces a gap that isn't fully closed by the current feature (a forward dependency onto a not-yet-built epic, something a future consumer needs to get right), file it as an actual backlog item in the relevant epic — not just a passing mention. See `backlog/002-identity-access/008-authdb-consumer-handoff.md` for the pattern.
- The design system (`docs/context/design-system.md`) is implemented as real code, not just a doc: dark-theme tokens live as CSS custom properties in `src/app/globals.css`, mapped into Tailwind v4 via `@theme inline` (no `tailwind.config.ts` — Tailwind v4 is CSS-config-first). The three Google fonts (Bricolage Grotesque/Hanken Grotesk/Spline Sans Mono) are self-hosted via `next/font/google` in `src/app/layout.tsx`, not the mockups' runtime `<link>` tags. The authenticated app is dark-only (no toggle in either app mockup); a `[data-theme="light"]` override exists in `globals.css` for the marketing landing page's toggle only — never apply it to the `(app)` route group. Shared presentational components (`Badge`, `Table`, `cn()`) live in `src/shared/ui/`, exported from its barrel; `components.json` at the repo root lets the `shadcn` CLI add further components aliased to that same location (not shadcn's default `@/components`).

- The GitHub repository was renamed from `SpecHub` to `SkillCanon` (canonical URL is now `github.com/mlopstapus/SkillCanon`) — discovered via a `git push` redirect notice (`This repository moved. Please use the new location...`). The local `origin` remote still points at the old `SpecHub` URL and still works (GitHub redirects pushes/fetches automatically), but any new public-facing link generated *from application code or docs* (e.g. the marketing page's GitHub/Docs links, `014-marketing-landing-page`) should point at the new `mlopstapus/SkillCanon` name, not the redirect. Don't infer the repo name from `git remote get-url origin` alone — verify against a real request (e.g. `curl -sI https://github.com/mlopstapus/<name>`) since the remote URL can silently lag a rename.
- **Blocking pre-hydration inline `<script>` tags (FOUC-prevention, e.g. a theme toggle) need care with React hydration.** `suppressHydrationWarning` on the mutated element only silences the console warning — it does **not** make React repaint a descendant whose server-rendered text/attributes now disagree with the script-mutated DOM, so a naive "read the DOM in a `useState` lazy initializer" approach leaves that descendant (e.g. a theme-toggle icon) visually stuck at its SSR value forever. The fix: keep the component's default state identical to what SSR renders (no lazy DOM read), then sync from the DOM in a genuine **post-mount** `useEffect` — that's a real client-side update, not a hydration reconciliation, so React actually commits it. Caught and fixed in `014-marketing-landing-page`'s `theme-toggle.tsx`.
- **A scoped `[data-theme="light"]` override only affects CSS custom-property resolution *within* the subtree it's set on — an ancestor's own literal use of that variable (e.g. `body { background: var(--bg) }` in `globals.css`) does not repaint**, since the ancestor sits outside the attribute's subtree. Any wrapper element meant to visually respond to a scoped theme attribute (e.g. `#marketing-root` in `marketing-shell.tsx`) must set its own explicit `background`/`color` from that variable rather than relying on inheriting the page's real background from `<body>` — otherwise toggling produces unreadable (correctly-recolored text over a still-wrong-colored background) content. Caught and fixed in `014-marketing-landing-page`.
- Any scroll-reveal / progressive-enhancement animation (fade/slide-in on viewport entry) must default to fully visible in its **server-rendered** markup, never `opacity:0` — the source Claude design mockups' own `data-reveal` elements hardcode `style="opacity:0"` and rely entirely on their own JS to ever reveal anything, which silently fails the "content must be readable without JS" requirement if ported literally. Correct pattern (see `014-marketing-landing-page/reveal.tsx`): default state `visible=true`; only flip to hidden inside a mount effect once JS is confirmed running (with a `prefers-reduced-motion` bypass and a safety-net timer), never in the initial render.
- `eslint-plugin-react-hooks`'s `react-hooks/set-state-in-effect` rule (active in this repo's flat config) flags **any** direct `setState` call in an effect body, including the common "sync React state from an external system once on mount" pattern (reading `localStorage`, a DOM attribute, or `matchMedia` that isn't itself a subscribable event source). This is a real, legitimate exception to the rule, not a bug to route around structurally — silence it with a same-line `// eslint-disable-next-line react-hooks/set-state-in-effect -- <specific external system>` rather than restructuring the effect just to satisfy the linter.
- This machine's own `docker compose` stack for this repo (`spechub-app-1`/`spechub-database-1`) commonly already occupies port 3000 from a prior session — `pnpm dev` falls back to 3001 automatically and that's expected, not a sign of an unrelated project's port collision (see `[[user_concurrent_docker_projects]]` for the cross-project case). Check `docker compose ps` / the container name before assuming a port conflict means another project is in the way.
- In Multica workspaces, Multica itself may occupy SkillCanon's default Compose ports (`multica-postgres-1` on 5432, `multica-frontend-1` on 3000). For a non-disruptive Docker finish check, use a temporary Compose override with `ports: !override` to map SkillCanon to alternate host ports such as `5434:5432` and `3001:3000`; do not stop Multica containers.

- For an explicit alternate Next dev-server port, use `pnpm exec next dev -p <port>`; `pnpm dev -- --port <port>` is parsed by Next 16 as a project directory and fails.
- The canonical SkillCanon brand mark (per the Claude Design mockups) is a specific SVG icon — a teal accent bar plus three horizontal bars of descending width/opacity, in a `--surface2`-background/`--border2`-border rounded box — not a plain letter "S". It's implemented once as `LogoMark`/`Wordmark` in `src/shared/ui/logo.tsx` (exported from `src/shared/ui`'s barrel) and used by the marketing nav/footer, the auth brand rail, and the app-shell sidebar as of `015-auth-onboarding-ui`. Any new surface needing the brand mark should reuse this component, not re-implement a letter-in-a-box placeholder — three separate surfaces had drifted to their own "S" letter (or, for the app-shell, no icon at all) before this was caught and fixed.
- `pnpm audit --audit-level=high` flags a real advisory in `brace-expansion` (GHSA-mh99-v99m-4gvg), reached transitively via both `eslint>...>minimatch@3.1.5` and `testcontainers>...>minimatch@9.0.9`/`5.1.9`. Do **not** fix this with a broad `pnpm.overrides` bump to `brace-expansion@>=5.0.8` — `minimatch@3.1.5`'s code calls a `brace-expansion` API (`.expand`) that doesn't exist in the 5.x line, so the override breaks ESLint outright (`TypeError: expand is not a function`) rather than fixing anything. This is a devDependency-only exposure (`brace-expansion` never ships in the `.next/standalone` runtime bundle) — treat it as an accepted risk pending an upstream `eslint`/`testcontainers` bump, not something to force-patch locally.
- Unlike `brace-expansion` above, `js-yaml` (via `eslint>@eslint/eslintrc`) and `nanoid` (via `postcss`) advisories found 2026-08-11 were safe one-patch-version bumps with no API break — fixed via `pnpm.overrides` (`js-yaml": ">=4.3.1"`, `"nanoid": ">=3.3.17"`) plus bumping the existing `postcss` override to `>=8.5.23` for a separate, later moderate-severity advisory found in the same pass. `pnpm audit --audit-level=high` (and `--audit-level=moderate`) is clean as of this fix. Don't assume every audit finding needs the brace-expansion treatment — check whether the fix version is actually API-compatible (a patch bump usually is) before reaching for "accepted risk."
- To visually verify a specific auth terminal/conditional UI state (e.g. register's "already set up" blocked view, an invite's expired/revoked view) when there's no live route or query param that triggers it — and the real page redirects away before you can reach it (e.g. `resolveAuthPageAccess` bouncing an authenticated session off `/register`) — temporarily replace that route's `page.tsx` with a `"use client"` component that renders the `*FormView` directly with the desired `state`, screenshot it, then revert the file. Passing a plain inline function as a prop straight from a Server Component throws "Functions cannot be passed directly to Client Components" even if the branch that would use it never runs, so the whole temporary page must be a Client Component, not just the prop value.

- Drizzle's runtime migrator reads `drizzle/migrations/meta/_journal.json`; a standalone `.sql` file added under `drizzle/migrations/` is ignored by Testcontainers migrations unless the matching journal entry exists. `pnpm db:generate` also creates a snapshot; a hand-written SQL-only migration still applies in tests once the journal tag points at it.
- A `pnpm db:generate` run in an environment with a skewed system clock can write a `when` epoch-millis value into `_journal.json` that's *earlier* than the immediately preceding entry (e.g. `018-prompt-version-model`'s `0013_prompt_registry_prompts` entry generated with a `when` a full year behind `0012`'s). This doesn't break migration ordering (drizzle uses array position, i.e. `idx`, not `when`), but check the new entry's `when` against the previous one before committing and bump it forward if it's out of sequence — otherwise the journal's own chronology is misleading for anyone reading it later.
- Running project commands via bare `npx`/`npm` (rather than `corepack pnpm`) in a Multica-managed workdir writes a stray root `package-lock.json` even though this repo is pnpm-only (`pnpm-lock.yaml` is the real lockfile) — delete it before committing; it's not `.gitignore`d because a real npm-based project would want it tracked.

- **Missing `drizzle/migrations/meta/NNNN_snapshot.json` files are a recurring gap, not a one-time bug** — fixed twice already (`backlog/done/000-foundations/archive/011-fix-missing-migration-snapshot-files.md`, originally for `0007/0008/0010/0011/0013`; backfilled again 2026-08-12 for `0025/0028/0029/0030`, which had silently reappeared). Root cause both times: `pnpm db:generate` diffs against the *latest snapshot file it can find on disk*, not true current schema state — any migration whose snapshot never got committed (most often a hand-written migration for an ambiguous rename, per the `pushSchema`/`generateMigration` interactive-prompt note elsewhere in this file) leaves every *later* `pnpm db:generate` silently unaware of it, bundling in already-applied DDL as a bogus "new" diff. **Detection heuristic**: if a freshly-generated `.sql` contains a `CREATE TABLE`/`ALTER TABLE` for something that already exists in the database, check `ls drizzle/migrations/*.sql` vs `ls drizzle/migrations/meta/*_snapshot.json` for a numbering gap before assuming your own change is wrong. **To backfill a missing snapshot**: use `generateDrizzleJson(allSchemaTables, prevSnapshot.id)` (from `drizzle-kit/api`) against the schema files' content *as of the commit that introduced that migration's `.sql`* (`git show <sha>:<path>`, temporarily swapped into the working tree via the Write tool, restored via `git checkout --` after), verify with `generateMigration(prev, candidate)` that the diff matches the real committed `.sql` file exactly (or, for an ambiguous rename `generateMigration` can't resolve non-interactively, verify structurally by comparing each table's column set instead), then fix up the *next* existing snapshot's `prevId` to chain to the newly-backfilled one. Confirm the fix with `pnpm db:generate` reporting "No schema changes, nothing to migrate" against an unmodified schema.
- When creating a new feature branch via `git checkout -b <name> origin/main` (or any `origin/<base>`), the branch's upstream tracking is set to `origin/<base>` directly (confirmed via `git config branch.<name>.merge` → `refs/heads/main`), **not** a same-named remote branch — a bare `git push`/`git push --force-with-lease` at that point targets `main`, not a new branch. Push explicitly with `git push -u origin <name>` the first time to fix tracking before relying on a bare push.
- For a bounded context whose table is genuinely new (first row of that schema, e.g. `distribution.prompt_usage` in `024-project-usage-metrics-dashboard`), generate and commit the Drizzle migration *before* writing the Testcontainers-backed tests that touch it — `startTestDb()` applies `drizzle/migrations/` literally, so a test against a `schema.ts`-only (not yet migrated) table fails at runtime with `relation "..." does not exist`, not a type error, even though `pnpm typecheck` passes cleanly.
- When a feature requires "A must never call B" as a hard invariant (e.g. `expand()` must never trigger `recordPromptUsage`, per `024-project-usage-metrics-dashboard`'s FR-002a), add a regression test that directly measures the invariant — call A, compare B's state before/after — rather than relying on code review alone to keep it true across future changes.
- A `"use client"` component importing any real (non-type) value from a bounded context's barrel (`@/bcs/<name>`) — not just `import type` — pulls that BC's *entire* server-side dependency graph into the browser bundle. `identity-access`'s barrel re-exports `createApiKey`/`inviteUser`, which transitively import `nodemailer`/`postgres`; importing e.g. `isScopeAllowedForRole` (a plain pure function) from a client component still drags those in and fails `pnpm build` with `Module not found: Can't resolve 'tls'` (a Node built-in with no browser equivalent). Fix: duplicate small, stable pure rules locally in the client component (with a comment noting what it mirrors and why) rather than importing them — the real enforcement should already live server-side anyway. `pnpm build`/`docker compose build` catches this; `pnpm typecheck`/`pnpm lint`/`pnpm vitest` do not, since none of them do a real client/server bundle split — always run a real build before considering a client-component-touching feature done.
- A client component that needs `useRouter()` (or any other hook requiring Next's App Router context) should not call it directly if the component also needs `renderToStaticMarkup` structural tests — that context doesn't exist outside a real Next.js render tree, and the hook call itself throws (`invariant expected app router to be mounted`), not just its usage. Split into a pure `<Name>View` (takes the hook's value as a prop, e.g. `refresh: () => void`) and a thin `<Name>` wrapper that calls the hook and passes it down — `app-navigation.tsx`'s `NavigationList`/`AppNavigation` established this pattern first; `teams-explorer.tsx`'s `TeamsExplorerView`/`TeamsExplorer` and `api-keys-list.tsx`'s `ApiKeysListView`/`ApiKeysList` are the second and third instances. Test the `View` component, never the outer wrapper.
- A client-rendered tree/hierarchy UI (team tree, org chart, folder view, etc.) must order rows by actual tree traversal (parent, then all its children recursively, then the next sibling) — never `teams.sort((a, b) => a.name.localeCompare(b.name))` across the whole flat list with only *indentation* reflecting depth. A flat alphabetical sort silently scatters a parent from its own children whenever an unrelated node's name happens to sort between them (caught in `019-account-team-settings-ui` only via live manual browser testing — small 2-3-node test fixtures never had enough siblings for the bug to surface in automated tests). Depth-first traversal with alphabetical-among-siblings is correct: group by `parentId` first, sort each group, then recurse from the roots.
- A React tab component whose panels are conditionally unmounted (`{tab === "x" && (...)}`) is untestable via this repo's `renderToStaticMarkup`-only testing convention for any tab besides the default — there's no jsdom/click-simulation to switch tabs first. Always render all panels and toggle visibility with a `hidden`/conditional className instead; this is also a defensible accessibility default (content present in the DOM, not re-fetched on every tab switch), not just a testability workaround.
- A URL-query-string-driven filter UI (filters live in `searchParams`, a server component re-runs on every change) must debounce any free-text input before calling `router.push`/`replace` — a per-keystroke navigation is a full server round trip, and typing faster than that round trip completes drops characters (only the last keystroke "wins" the race). Keep the text field's value as local component state, debounce the actual navigation call (~300-400ms), and only resync local state from the URL-derived prop when no debounce is currently pending — otherwise an unconditional resync-on-prop-change reintroduces the same race. Caught in `020-audit-log-ui`'s search box only via live manual browser testing (`renderToStaticMarkup` can't simulate real per-keystroke timing at all).
- A list page with both a "no data at all" and a "no data matches the current filter" empty state must derive both from `hasActiveFilters(filters)`, not from `total === 0` alone — the query result's `total` is always the *filtered* count (no separate unfiltered org-wide count is ever fetched), so using it by itself for both checks means a zero-result filtered search always shows the "no data at all" copy, never the "no match" one. Caught in `020-audit-log-ui` via live manual browser testing; the original unit test coincidentally passed because its fixture used an unrealistic `total` value that no real caller would ever produce (see `audit-log-view.test.tsx`'s note on this).
- `identity-access`'s `getUser(db, userId, organizationId?)` deliberately never returns `displayName` (its own contract: "never `username`, `display_name`, `is_active`, `password_hash`, or timestamps") — resolving a bare user id to a human-readable name must go through `listUsers(db, actingUser)` and filter by id instead (the same pattern `list.ts`'s `resolveActorUserIdsByDisplayName` already used for search), not `getUser`. `020-audit-log-ui`'s resource/actor-name resolvers hit this directly.
- `audit-compliance`'s retention-days and export-entitlement resolution now goes through `resolveAuditEntitlementsForOrg(organizationId)` (in `application/resolve-audit-entitlements-for-org.ts`), which calls the real `billing-entitlements.resolveEntitlements(orgId)` — not the old same-BC-only hardcoded domain stub `resolveAuditEntitlements()` (removed in `020-audit-log-ui` after `/speckit-analyze` found `billing-entitlements` already existed, and its own `CONTRACT.md` already listed "Audit & Compliance" as an intended consumer that had just never been wired up). `canExportAuditEvents` still stays hardcoded `false` — no export-specific key exists yet in `EntitlementSnapshot`. Any future `audit-compliance` code needing entitlements should call this function, not reintroduce a local stub.
- No human-readable plan/tier name exists anywhere in this codebase yet — not on `identity_access.organizations` (`planId` is a nullable UUID FK to a not-yet-built `plans` table), not in `billing-entitlements`' `EntitlementSnapshot` (flags/limits only, no name field). A UI surface wanting to show "which plan is this" today can only show resolved numeric/boolean entitlement values (e.g. retention days), never a fabricated "Free"/"Paid" string — `020-audit-log-ui` caught its own draft doing exactly this (using `organizations.planId` as a display label) via `/speckit-analyze`, before it shipped.
- Running a full Testcontainers-backed `pnpm test` while another Claude Code session is doing the same against this repo can exhaust Docker Desktop entirely (`Cannot connect to the Docker daemon`) for both sessions — each test file spins up its own Postgres container, and two full suites in parallel can be 100+ containers. Check for another session's activity (or just ask) before kicking off a full-suite run, and don't unilaterally restart/rebuild Docker to recover — that tears down whatever the other session has running too. **Distinguish "saturated but alive" from "daemon actually crashed"**: if `docker ps` fails with the daemon itself unreachable (not just slow/timing out), the above caution doesn't apply — there's nothing left to tear down, since the crash already took it down for every session. Recover with `open -a Docker`, then poll (macOS ships no `timeout` command by default — use a manual bash loop: `i=0; until docker ps >/dev/null 2>&1 || [ $i -ge 30 ]; do sleep 3; i=$((i+1)); done`) rather than a long blocking sleep.
- A Speckit spec can go stale mid-flight if its originating backlog item gets redesigned by a different concurrent session/branch after `/speckit-specify`+`/speckit-clarify` already ran against the old version (happened to `020-prompt-sharing`, re-specified after PDR-016 redesigned `006-prompt-registry/003-prompt-sharing.md` out from under it). Symptom: the spec's own `Input` field literally quotes backlog text that no longer matches the file on disk. Fix by `git merge origin/main` into the spec's branch first (to pull in whatever redesign already landed), then re-run `/speckit-specify` with an explicit "supersedes the original input, here's what changed and why" description — write the **same** feature directory in place rather than minting a new numbered one, since it's the same backlog item/branch/scope, just a different mechanism.
- Before trusting an epic's `EPIC.md`/backlog-item `status: open`/`not-started` field at face value, `grep` for real `application`/`infrastructure` files under that bounded context — this repo has repeatedly had actual shipped, migrated code (e.g. `governance`'s full policy/objective CRUD + resolution engine, `prompt-registry`'s full prompt/version CRUD) that predates or contradicts what the tracking file claims, discovered only by checking the filesystem directly.
- `prompt_registry` **now has Postgres RLS on all its tables** (`0012_prompt_registry_projects.sql` covered `projects`/`project_members` from the start; `0019_prompt_registry_rls.sql`, from `022-prompt-registry-tenant-isolation`, added the rest — `prompts`, `prompt_versions`, `subscriptions`, `project_teams`, `project_skill_assignments`). A prior note here claiming "no RLS at all, deliberate" is now stale — that was accurate only up through `018`/`021`; verify current RLS coverage by checking for `*_rls.sql` migrations and each table's own `CREATE TABLE` migration, not by trusting an older note in this file. Practical effect: any other BC calling into `prompt-registry`'s exported reads (e.g. `getProject`) must already be running inside a transaction where `withTenantContext` set `app.current_org_id` — see the `023-workflow-model-crud` entry below for how a new BC discovers and handles this.
- **A bounded context calling into a *different* BC's exported function that reads/writes an RLS-protected table must let the composition root (caller/test) establish tenant context — it must not call `withTenantContext` itself.** No application-layer function anywhere in this codebase calls `withTenantContext` internally (confirmed by grep) — it's only ever called from a route handler, MCP handler, or a test, wrapping the *entire* call chain including any nested cross-BC calls. `023-workflow-model-crud`'s `createWorkflow` needed this: it calls `prompt-registry`'s exported `getProject(db, orgId, projectId)` to enforce the "project must belong to the caller's org" invariant, and `prompt_registry.projects` has been RLS-protected since `0012`. `createWorkflow` itself never calls `withTenantContext` (matching the codebase-wide convention) — every test invoking it wraps the whole call in `withTenantContext(testDb.appDb, orgId, tx => createWorkflow(tx, ...))`, exactly like `prompt-registry`'s own `createProject`/`createPrompt` tests already do. Forgetting this wrapper doesn't silently return empty results — Postgres raises `unrecognized configuration parameter "app.current_org_id"` outright, since RLS's `USING` clause calls `current_setting(...)` without the `missing_ok` argument.
- `identity-access`'s org-admin-or-team-owner authorization check (`assertCanManageInvitationsForTeam`, used internally by `inviteUser`/`revokeInvitation`/`removeTeamMember`) is **not** exported from its barrel — a different BC needing the same "is this caller allowed to act for this team" rule (e.g. `prompt-registry`'s `020-prompt-sharing` feature) must re-derive the two-line check locally (`actingUser.role === "admin" || (await getTeam(...)).ownerId === actingUser.id`) using `getTeam`, which *is* exported, rather than importing the internal helper. Same pattern as the already-documented `isScopeAllowedForRole` non-export, different function.
- **Nunjucks** (`nunjucks` + `@types/nunjucks`) is now a real dependency (added by `021-expansion-engine`) — the backlog's own "Jinja2 → Nunjucks per PDR-001's assumption" framing is misleading: PDR-001 doesn't actually mention templating at all. Treat that citation as informal, not authoritative, if it resurfaces elsewhere. Nunjucks has no literal `SandboxedEnvironment` class the way Python's Jinja2 does — "sandboxing" here means minimal surface area (`throwOnUndefined: true`, and never registering any template global beyond what a feature explicitly needs, e.g. `include_prompt`) rather than a distinct security mode to enable. `src/bcs/prompt-registry/infrastructure/template-renderer.ts` is the one file that constructs a Nunjucks `Environment` — no other file should.
- When delegating a single-feature implementation to a subagent, **check `git status` for changes outside that feature's own files before trusting a "done" report** — an implementation agent for `021-expansion-engine` archived *every* backlog item across two entire epics (005-governance, 006-prompt-registry) it happened to notice were "actually done," not just the one item it was asked to build, and did so while leaving each file's own `status:` frontmatter still at `open` (a self-contradictory state this repo's archive convention never allows). Caught and fully reverted before it landed — verify an agent's file-level diff matches its own stated scope, especially for anything that moves/deletes files, not just the specific feature files you expect it to touch.

- `src/bcs/prompt-registry/application/authorize-owner-action.ts`'s `assertAuthorizedForOwner` (org-admin-or-team-owner check, built for `020-prompt-sharing`'s subscribe/fork) turned out reusable as-is for `022-project-skill-assignment`'s completely different "is this user authorized to administer this project's owner team" rule — same underlying check, different caller. Before writing a new team-authorization helper anywhere in this bounded context, check whether this one already covers it.
- An audit-event test assertion scoped only by `action = '...'` silently picks up a *different* test's row in the same file/`describe` block, since the shared `audit.audit_events` table is never reset between tests — caught in `022-project-skill-assignment`'s `remove-collaborator-team.test.ts`. Scoping by `resource_id in (select ... where <fixture-condition>)` doesn't help for a *rejected* mutation specifically, since no row (and thus no resource_id) was ever created to scope against. Fix: scope by `organization_id = <the fixture's own fresh randomUUID() org>` instead — safe in every case, successful or rejected, since each fixture always creates its own org.
- Before assuming a backlog item is only *partially* satisfiable by a feature that pulls part of it forward (per the established pull-forward precedent above), check afterward whether the pulled-forward piece actually closes out the *entire* backlog item, not just the part that motivated the pull — `022-project-skill-assignment` pulling forward `001-project-model-and-membership`'s collaborator-team piece turned out to fully satisfy every one of `001`'s requirements (the rest had already shipped separately via PR #39), so `001` got archived too, not left partially open.

- **An application function can never both throw an error *and* durably persist a "this failed" marker in the same call** — `db`/`tx` handed into every application function is always the caller's own already-open outer transaction (every real caller and every test wraps the call in `withTenantContext`, which is `db.transaction(...)` under the hood), so any thrown error rolls back *everything* written earlier in that same call, including a status-update write meant to survive the throw. Discovered in `026-skill-chains`'s `advanceSkillChainRun`/`startSkillChainRun`: the first design tried to mark a chain run `"failed"` and then still throw `ChainStepResolutionFailedError` — a failing test (not code review) caught that the "failed" row never actually persisted. Fix: don't try to persist a failure marker in the throwing call at all — let the whole call roll back like any other validation failure (e.g. `InvalidChainDependencyError`), and treat the thrown error itself (structurally distinct from any normal return value) as the "distinguishable failure signal," never a DB row. If a future feature genuinely needs a failure to survive an error being thrown from the same call, it needs a second, independently-committed transaction on a *different* connection — which no application function in this codebase does today, since it would mean not accepting `db` as a parameter, a real precedent-breaking choice, not a small tweak.
- **A concurrent-safe mutation that acts on "whatever is currently pending" needs the caller to name the specific state it's targeting — a `SELECT ... FOR UPDATE` row lock alone only serializes *access*, not *intent*.** `026-skill-chains`'s `advanceSkillChainRun` originally took an opaque `{status, output?, error?}` report with no reference to which step it was for; two racing (or a stale network-retried duplicate) calls were indistinguishable once the row lock let the second one through — it would silently get applied to whatever step had newly become current, not rejected as a conflict. Fixed by adding a required `stepIndex` to the report and comparing it against the run's actual current step under the lock. Caught while writing the concurrency test, not by using the row lock in production — a Promise.allSettled test firing two truly simultaneous calls at the same pending step is what surfaced it; a sequential "call twice in a row" test would not have (the second call would trivially see a different current step and could look like it "worked" for the wrong reason).
- Nunjucks' `StrictUndefined`-equivalent rendering (`throwOnUndefined: true`, `template-renderer.ts`) rejects outputting `null` directly with `attempted to output null or undefined value` — the same error as a truly undefined variable, not just a blank render. The `default` filter's two-arg form (`{{ x | default("fallback") }}`) only substitutes for `undefined`, **not** `null` — the three-arg form with the boolean flag (`{{ x | default("fallback", true) }}`) is required to also catch `null`/`false`/`0`/`""`. Matters anywhere a template renders a value that can legitimately be `null` by design (e.g. `026-skill-chains`'s per-step `dependsOn` envelope, `{ status: "error", output: null }`) — the two-arg form silently breaks exactly the failure-path templates most in need of a safe rendering, since success-path testing alone won't exercise it.
- When a function's parameter shape changes (e.g. `insertPromptVersion` gaining required `kind`/`steps` fields), grep for every call site by import path across the whole BC before assuming you've found them all — `fork-skill.ts` had its own separate hand-built call to `insertPromptVersion` (copying a source version's fields into a new one) that a search scoped to "the obvious caller" (`publish-version.ts`) would miss entirely. TypeScript's own compiler error at the missing-fields call site is what actually caught it in `026-skill-chains`, not a manual grep — treat a `tsc --noEmit` pass after any shared-function signature change as a real correctness check, not just a formality, especially for a function with more than one caller.
- Tightening a previously-permissive validation rule in a widely-called shared function (e.g. `publishVersion` starting to reject "neither template content nor chain steps given," per `026-skill-chains`'s FR-001) has blast radius across *pre-existing* tests throughout the BC, not just new ones — 8 already-passing test files elsewhere in `prompt-registry` (`fork-skill.test.ts`, `list-versions.test.ts`, `rollback-prompt.test.ts`, `subscribe-skill.test.ts`, `tenant-isolation.test.ts`, etc.) called `publishVersion` with no template content at all as a "don't care about content" shorthand, and all failed once the stricter check landed. Before shipping a stricter validation rule on a function with many callers, grep every call site repo-wide first (`grep -rn "publishVersion("` or equivalent) rather than assuming the new tests written for the change are the only ones affected.

- **`027-rest-api-core-routes` established the REST composition-root pattern** (`src/app/api/**/route.ts` + `src/shared/api/{auth,errors,handler,pagination,test-helpers}.ts`) — reuse this, don't reinvent it, for any future route: every route file exports named raw handlers (`handleGet`/`handlePost`/etc., signature `(request, { caller, params, db }) => Promise<Response>`) plus `export const GET = withApiRoute(handleGet)` at the bottom. `withApiRoute(fn, deps?)` resolves the caller (session cookie or `Authorization: Bearer`, dual-mode per FR-010), maps thrown errors via a name-keyed registry (`src/shared/api/errors.ts`, ~60 existing domain error classes — no `DomainError` base-class retrofit), and injects `db`/`authDb` into the handler's context. **A route handler must never `import { db }`/`import { authDb }` from `@/shared/db` directly** — those singletons are lazily-initialized, env-var-keyed, and have no test seam; a route that closes over them directly is untestable against Testcontainers. Always destructure `db` from the handler's own context (production defaults to the real singleton; `route.test.ts` overrides via `withApiRoute(handler, { authDb: testDb.authDb, db: testDb.appDb })`).
- Two shared-module gaps between `docs/context/api-conventions.md`'s documented design and the actual code, both closed by `027-rest-api-core-routes`: `src/shared/logging` only exported a bare `createLogger`/`logger` (no `getLogger(bc)` — now added, a `pino` child-logger factory); `identity-access`'s barrel (`src/bcs/identity-access/index.ts`) exported **none** of its own ~14 domain error classes, unlike `governance`'s and `prompt-registry`'s barrels, which already did (now fixed, matching the sibling BCs' convention). Before assuming a documented convention already exists in code, grep for it.
- `identity-access`'s `getTeam`/`getUser`/`updateTeam`/`insertTeamBetween`/`reparentTeam` throw a bare, untyped `new Error('No <resource> found with id "...".')` for "not found" — no `TeamNotFoundError`/`UserNotFoundError` class exists anywhere in the BC (unlike `updateUser`/`deactivateUser`, which correctly throw the registered `CrossOrgUserAccessError` for the identical situation). A generic consumer (e.g. a REST error mapper) can't `instanceof`-match this — the only safe handling is a narrow, call-site-scoped catch checking `err.message.startsWith("No team found with id")` (each function's only throw path), documented at each call site, not a blanket "any thrown error means not-found" catch.
- `expand()`'s Nunjucks template variables come from `{ ...input }` **spread directly** into the render context, not nested under an `input` key — a template referencing `{{ input.topic }}` fails with Nunjucks' `attempted to output null or undefined value` (StrictUndefined rejecting the nonexistent `input` variable itself). The correct reference is `{{ topic }}`.
- `updateTeam` explicitly excludes hierarchy changes (its own doc comment: "never its hierarchy position") — a REST/consumer-facing "move team to new parent" capability needs `reparentTeam` via its own dedicated call/route, not folded into a generic team-update path.
- Several `prompt-registry`/`identity-access` function signatures don't match what an API-conventions-era doc or a first-pass contract sketch assumed — always verify against the actual source file before writing consumer code, not just `CONTRACT.md`/backlog prose: `createApiKey(db, actingUser, params, auditContext?)` has **no target-user parameter at all** (always creates a key owned by `actingUser` — a caller wanting to issue a key on someone else's behalf has no BC-level path); `createProject`/`createPolicy`/`createObjective` each require a mandatory identity/scope-**verifier callback object** (`ProjectIdentityVerifier`/`PolicyScopeVerifier`/`ObjectiveScopeVerifier`), not a bare params object; `createPrompt` has no `ownerType`/`ownerId` (a skill is always owned by its creating user, PDR-016); `insertTeamBetween(tx, params, childTeamId, actingUser, options)` takes `childTeamId`/`actingUser` as separate positional args, not nested inside `params`; `deletePolicy` is a soft delete (`isActive: false`) and `getPolicy` doesn't filter on `isActive`, so a GET after DELETE still returns 200 with the row.
- `getProjectMetrics`/`listProjectMembers`/`listProjectSkillAssignmentsForOrganization` (prompt-registry) don't check project existence themselves — they silently return empty/zeroed data for a nonexistent `projectId` rather than throwing or returning `null`. A REST route wrapping one of these for a single-project GET needs its own explicit `getProject` existence check first, or a bad id 200s instead of 404ing.
- Confirmed again in `027-rest-api-core-routes`: this machine's `docker compose` stack for this repo can have multi-day uptime (`skillcanon-app-1`/`skillcanon-database-1` observed at 4 days) — a `docker compose down`/rebuild for a change that doesn't touch `Dockerfile`/`docker-compose.yaml`/`database/` is genuinely disruptive to whatever else is using that stack. `pnpm build` (no Docker) is a good non-disruptive substitute to verify a Next.js route/bundle change compiles and is correctly discovered by the router, without tearing down the shared stack.

- Two concurrent sessions picked the same "next available" spec number independently (`027-rest-api-core-routes` and `027-skill-chain-views-ui`, both computed by scanning `specs/` at session-start time before either had merged) — the sequential-numbering convention has no reservation mechanism across sessions. Before finalizing a new spec directory's number at `/as-finish` time (after rebasing onto latest `origin/main`), re-check `ls specs/` for a collision, not just at `/speckit-specify` time — if one exists, rename the newer spec directory (`mv`, not `git mv`, if it was never committed/tracked yet) to the next free number and fix its own internal self-references (`Feature Branch`/path mentions in spec.md/plan.md/tasks.md), while leaving the actual git branch name alone (branch name and spec directory number are independently allowed to differ, per this repo's own convention).
- After `git rebase origin/main` (or any pull that lands a concurrent PR), run `pnpm install` before trusting `pnpm typecheck` — a new dependency added by that PR (e.g. `zod`, added by `027-rest-api-core-routes`) is in the rebased-in `package.json`/lockfile but not yet in local `node_modules`, producing a wall of `Cannot find module` errors that look like real type errors but are just a stale install.
- `drizzle/migrations/0024_drop_workflow_schema.sql`'s `DROP TABLE "workflow"."workflows" CASCADE` was fixed to add `IF EXISTS` (`backlog/done/000-foundations/archive/013-fix-non-idempotent-workflow-schema-drop-migration.md`) — this is now safe to re-run against a database whose history never actually created that table. Only relevant if you're looking at an *older* checkout of this migration file or debugging why a stale environment behaved differently before this fix landed.
- Grepping "every call site" of a shared function you're changing (per the existing entries above on this) is only complete as of the moment you run it — a concurrent PR rebased in *after* that grep can add a brand-new caller your search never saw. `listSkillChainRuns`' pagination-shape change (`028-skill-chain-views-ui`) was fully call-site-audited within `src/bcs/prompt-registry` at implementation time, but `027-rest-api-core-routes`'s `src/app/api/skills/[name]/chain-runs/route.ts` (added later, rebased in afterward) called it too and broke — only surfaced by actually running the full test suite post-rebase, not by re-trusting an earlier grep. Re-grep (or run the full/broad test scope) again after rebasing onto latest `main` during `/as-finish`, don't rely solely on the grep done during implementation.

- `029-skill-sync-cli` added the first non-`src/`, non-`legacy/` top-level package: `cli/` (the standalone `skillcanon` CLI, published separately from the main app per its own backlog item). It's a genuinely independent package — own `package.json`/`pnpm-lock.yaml`/`node_modules`, not a pnpm workspace member (no `pnpm-workspace.yaml` exists) — and is excluded from the root `tsconfig.json`'s `exclude` and `eslint.config.mjs`'s `ignores` (mirroring how `legacy/` is already excluded from both). Any future root-level typecheck/lint change should keep `cli/` excluded rather than trying to fold it into the main app's config; `cli/` has and needs its own `pnpm --dir cli run typecheck`/`pnpm --dir cli test` (documented in `.claude/anchorstack/project.md`'s `## Test` section), not a subset of the root commands.
- Real API keys (`identity-access`'s `create-api-key.ts:28`) are generated as `"sk_" + randomBytes(32).toString("base64url")` — prefixed with an **underscore**, not a hyphen, and ~43 characters after the prefix. Any future credential-redaction regex, log-scrubbing pattern, or format validation should match `sk_[A-Za-z0-9_-]{20,}`, not an assumed `sk-...` shape — a first-draft redaction backstop in `cli/src/redact.ts` originally guessed the hyphen form and would have silently never matched a real key; caught by `as-security-scan`, not by any test (the test suite's own fake fixture keys happened to use whichever prefix was written, so they didn't catch the mismatch either — only cross-checking against the actual generator code did).
- `POST /api/skills/[name]/expand`'s response body is `{ systemMessage: string | null, userMessage: string, appliedPolicies: string[], objectives: string[] }` (`ExpansionResult`, `src/bcs/prompt-registry/domain/expansion.ts`) — not a single flat "resolved text" string. Any future consumer (the CLI's `run` command, a future non-Claude-Code IDE adapter, etc.) needs to compose `systemMessage`/`userMessage` into whatever single output shape it needs; there is no pre-joined field to read off the response directly.
- The pattern "a resource's own identifier is its already-existing web UI URL, decomposed client-side (origin = API base, path segment = id) — no new server-issued token/endpoint" (used for `029-skill-sync-cli`'s CLI "project key", `<origin>/projects/<id>`) is a reusable option worth considering for any future feature that needs a human to hand a client tool "which server + which resource" in one paste, before reaching for a bespoke server-generated connection-token endpoint.

- **Enabling RLS on a table for the first time can break pre-existing tests that write to it via `testDb.appDb` directly, unwrapped in `withTenantContext`** — those calls relied on no tenant-context enforcement existing yet, and correctly start failing (`current_setting('app.current_org_id')` unset) once RLS lands. `030-distribution-tenant-isolation` enabling RLS on `distribution.prompt_usage` broke `src/bcs/prompt-registry/application/get-project-metrics.test.ts`'s several `recordPromptUsage(testDb.appDb, {...})` calls this way. Fix is always to wrap the call in `withTenantContext`, matching every other caller — never to weaken the new policy or skip the now-failing test. Grep the *entire* repo (not just the bounded context gaining RLS) for direct, unwrapped calls to the newly-protected table's write function before considering an RLS rollout complete; a full-suite run is the actual proof, since a grep can still miss an indirect call.
- Default-parallel `pnpm test` (`vitest run`) doesn't just run slowly on this repo's 240+ Testcontainers-backed test files — confirmed (`030-distribution-tenant-isolation`) it can **crash the Docker daemon outright** (100+ concurrent Postgres containers, `docker ps` becomes unreachable), producing ~100 files failing with "Timeout waiting for worker to respond" that look like mass regressions but are pure infra exhaustion. Never use bare `pnpm test` as a full-suite verification gate. Always use `.claude/anchorstack/project.md`'s documented `integration` command — `pnpm exec vitest run --fileParallelism=false --testTimeout=30000` (sequential, one Postgres at a time, ~15-20 min) — for any full-suite validation, including during `/as-finish`.
- A second confirmed instance of the "spec goes stale mid-flight" pattern (see the `020-prompt-sharing` entry above): `030-distribution-tenant-isolation`'s spec was drafted against `distribution.prompt_usage`'s pre-`004-usage-telemetry` schema, then `008-distribution/004-usage-telemetry` landed on `main` (via a concurrent Multica agent run, committed as its own independently-numbered `specs/001-usage-telemetry/`, not this repo's next sequential number) while the branch was being created — adding real columns, a new query function, and the feature's first real production callers. Caught only because creating the feature branch from `origin/main` pulled the new commit in immediately; the spec was corrected in place with a `**Supersedes**:` note (same feature directory, not a re-specify) per the existing precedent. A concurrent session's own spec numbering can start from `001` independent of this repo's sequence — don't assume a foreign `NNN-*` spec directory prefix means anything about this repo's own next-available number.
- A stale local `docker compose` dev volume whose migration history has diverged from the current `_journal.json` (for any reason — e.g. it predates a fix to an older migration file) can leave `pnpm db:migrate` failing in ways that look like an app bug but aren't. If `pnpm db:migrate` hits this on a **local** dev stack (not a shared/production one), `docker compose down -v` + `docker compose up -d` + re-`db:migrate` is a safe, fast recovery (confirmed working end-to-end in `030-distribution-tenant-isolation`) — local dev data has no value worth preserving over a working, fully-migrated stack.
- When a shared function's signature/return shape changes (e.g. `expand()` dropping `input`, collapsing `systemMessage`/`userMessage` into `content`, per `032-skill-file-format-refactor`), grep for every same-BC caller too, not just cross-BC/REST/MCP consumers — `026-skill-chains`' chain-step resolution (`start-skill-chain-run.ts`/`advance-skill-chain-run.ts`/`resolve-chain-step.ts`/`get-skill-chain-run.ts`/`skill-chain-run-steps-repo.ts`/`domain/skill-chain.ts`, plus its own `skill_chain_run_steps` table) called `expand()` internally and was missed on the first pass. Worse, it depended on `expand()`'s removed `input` parameter for a real feature (threading a prior step's caller-reported output into the next step's template via `{{ step1.status }}`/`{{ step1.output }}`) — a genuine design fork requiring a user decision mid-implementation, not just mechanical propagation. Resolved: dependency auto-substitution is dropped entirely — a chain step resolves with zero arguments, exactly like any other `expand()` call; a prior step's reported output stays visible to the *caller* via the run's step list, never auto-injected.
- The standalone `cli/` package (`029-skill-sync-cli`) is a **third** consumer of `expand()`'s REST contract (`cli/src/http/skillcanon-client.ts`'s `expandSkill`, `cli/src/commands/run.ts`) beyond REST/MCP — it calls the same `/api/skills/[name]/expand` endpoint and is invisible to root `pnpm typecheck`/`pnpm lint` (its own separate toolchain, per the `029` entry above). When `expand()`'s contract changes, check `cli/` explicitly; it won't surface as a root-level error, only as silent runtime breakage (a CLI command that used to print content prints nothing).
- Route tests that build request bodies as `JSON.stringify({...})` literals (e.g. `src/app/api/skills/[name]/versions/route.test.ts`) are **not** typechecked against the domain param type they'll eventually be parsed into — `pnpm typecheck` passing is not proof these bodies still match a changed shape. Two such tests kept sending the pre-`032-skill-file-format-refactor` `systemTemplate`/`userTemplate` fields and only surfaced as real assertion failures (422 instead of 201, empty list instead of one row) during the actual `pnpm vitest run`, not typecheck. When a request-body-shaped domain type changes, grep raw `JSON.stringify(` call sites across `*.test.ts` files too, not just TS-checked object literals.
- A Nunjucks default-fallback string that references a variable which stops existing (e.g. legacy `userTemplate ?? "{{ input }}"`, from before `expand()`'s `input` param was removed) is a self-inflicted `StrictUndefined` crash the moment that variable leaves scope — distinct from the already-accepted PDR-018 risk of a skill *author's own* `{{ var }}` usage in stored content. Any synthetic/generated default template string (not author-typed) should default to a plain literal (`""`) instead of continuing to reference a variable that's being removed in the same change.
- `drizzle-kit generate` can't resolve an ambiguous "did you rename column X→Y, or drop X and add Y" case non-interactively (`Error: Interactive prompts require a TTY terminal`) — happens when a single schema-diff swaps two+ columns for one differently-named one (e.g. `skill_chain_run_steps.system_message`/`user_message` → `content`). When this isn't a real rename, hand-write the migration SQL directly (`ALTER TABLE ... DROP COLUMN`/`ADD COLUMN`) and manually append the `_journal.json` entry (same already-accepted pattern as this repo's missing-snapshot-files gap) rather than fighting drizzle-kit's prompt.

- `prompt_registry.prompts` (the roster row a skill's `GET /api/skills` response item is built from) has **no `kind` column at all** — `kind` (`"template" | "chain"`) lives only on `prompt_versions`, since different versions of the same skill can have different kinds. `033-skill-file-format-cli-support`'s planning docs (research.md/data-model.md) wrongly assumed a roster item carried `kind` directly, alongside the real `activeVersionId` field; the CLI's own production code never actually read `skill.kind` (it correctly used the matched version's `kind` from `getSkillVersions()`), so this was a dead/misleading type-only bug, not a runtime one — caught only by grepping the real `prompts`/`prompt_versions` schema and route handlers during final manual validation, not by any test (mocked test fixtures had encoded the same wrong assumption). When a CLI/client-side type claims a field exists on a specific REST response shape, verify against the actual route handler + schema before shipping, not just against a first-pass research.md guess — same lesson as the `027-rest-api-core-routes` entry above, another instance of it.
- Enabling RLS on `audit.audit_events` for the first time (`003-audit-compliance/004-audit-events-rls`) had a **much larger blast radius** than any prior RLS rollout in this repo — nearly every mutation across every bounded context writes an audit event via `withAudit()`/`record()`, so pre-existing tests reading/writing that table via `testDb.appDb` directly (unwrapped in `withTenantContext`) broke across **four** BCs at once: `audit-compliance`'s own tests, plus `identity-access` and `governance`'s tests and shared test-helper modules (`objective-test-helpers.ts`, `policy-test-helpers.ts`). `prompt-registry`'s equivalent shared test-helpers already read via `testDb.ownerDb` (the migration superuser, which bypasses RLS regardless of `FORCE ROW LEVEL SECURITY`) and needed no changes — prefer that pattern for any *new* shared audit-read test helper going forward, since it's immune to this exact class of breakage. When adding RLS to a table this central, budget for a full-repo grep (not just the owning BC) and expect the fix to span many files, not a handful.
- A null-`organization_id` row in an RLS-protected table (e.g. `audit_events`' pre-auth failed-login events) is **structurally invisible** to any role's policy using a direct `organization_id = current_setting(...)::uuid` equality predicate — a null value never equals a UUID, no matter what the session variable is set to. This is usually *not* a new restriction to design around; check whether the application layer already implicitly excluded null rows via the same equality filter before RLS existed (it did here) before treating it as a gap needing a special-case policy.
- `getJwtSecret()` (`src/shared/config/index.ts`) rejects both an empty value *and* any value containing the literal substring `REPLACE_ME` — a zero-config Docker Compose default for `JWT_SECRET` must be a real (if dev-only) string like `changeme_in_production`, not `REPLACE_ME_JWT_SECRET` (the `.env.example` placeholder), which would still throw. Same pattern already established for `APP_DB_PASSWORD`/`AUTH_DB_PASSWORD`'s `docker-compose.yaml` defaults.
- A `docker compose up -d --build` can hang indefinitely (45+ minutes, zero containers ever produced) if a `docker system prune` runs concurrently mid-build, clearing layer cache out from under it — especially likely on this machine given the number of concurrent unrelated Docker projects already running (see `user_concurrent_docker_projects` memory). If a compose build seems stuck for this reason, kill it and fall back to a lighter-weight verification (direct unit-level check of the underlying validation function, `docker compose config` for pure YAML/interpolation correctness) rather than waiting indefinitely — both are real, conclusive checks for a config-only change that doesn't touch application logic.
- The long-lived shared `spechub-app-1` dev container can be running with env config that predates a `docker-compose.yaml` fix (confirmed 2026-08-09: `JWT_SECRET`'s `${JWT_SECRET:-changeme_in_production}` fallback existed in the file but the running container's own `docker compose exec app env` showed no `JWT_SECRET` at all) — every login attempt 500s with `Error: JWT_SECRET is missing` in `docker compose logs app`, unrelated to any auth-flow code change. Fix: `docker compose up -d app` (recreates just the app container from current compose config, no `--build`/image rebuild, doesn't touch the database container) — safe and fast, not the disruptive full-stack rebuild this file otherwise warns against.
- Browser (Chrome via claude-in-chrome) HTTP/page caching in this environment can be aggressive enough that a plain `navigate` to an already-visited URL serves a stale cached document — including one whose embedded Next.js Server Action reference IDs point at a build that no longer exists after an app container recreate (`Error: Failed to find Server Action "..."` / generic "This page couldn't load"), or one still rendering a different, previously-logged-in user's session server-side despite a fresh login in the same tab. Append a cache-busting query param (`?_cb=N`, incrementing) on navigation whenever testing a server-action-backed form or checking post-login state after any container restart or account switch — a bare re-navigate to the same URL is not reliable evidence of current server state.
- Typing into a login/email field via `computer` tool's `type` action can interleave with Chrome's own autofill suggestion for that origin (a previously-saved, unrelated credential), corrupting the typed value (observed: `alice@example.com` + a stray saved Gmail address concatenated into one string). `triple_click` + `ctrl+a` + `Delete` before typing does not reliably clear this either. Reliable fix: set the value via JS using the input element's native value setter (`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, "value").set`) then dispatch `input`/`change` events — a raw `el.value = "..."` is silently ignored by React-controlled inputs, and this approach bypasses autofill interference entirely.
- `computer` tool screenshot pixel coordinates can be measurably offset from the page's real DOM layout coordinates in this environment (observed a ~30-45px vertical offset between a screenshot's apparent button center and the button's actual `getBoundingClientRect()`) — a click at the visually-correct center can land just outside the real element and silently do nothing (no network request in `docker compose logs`, no console error, page unchanged). If a click produces zero observable effect, verify the target's real bounding rect via `javascript_tool` before concluding the app itself is broken; `element.click()` via JS is a reliable fallback once the right element is confirmed.
- An org admin does **not** automatically have "accessible" access (the owner/team/subscription/project-membership set `assertSkillAccessible`/`listPrompts` compute) to a skill owned by a different individual user — even same org, same team. `workflows:run`-scoped API key issuance is admin-gated, but *running* a specific chain still 404s (`PromptNotFoundError`, indistinguishable from not-found per this repo's established cross-org-denial convention) if the admin isn't in that skill's accessible set. For ad hoc testing, "Make a copy" (fork) on the skill's detail page is the practical way to get a run-ready copy into the admin's own accessible set without altering the original owner's data.
- `src/shared/ui/drawer.tsx`'s `Drawer` primitive (added 2026-08-09) is now the required pattern for any new slide-in panel — it closed a repo-wide gap where all 15 pre-existing hand-rolled drawers had zero `role="dialog"`/`aria-modal`/focus trap/Escape-to-close (found via a live screen-reader-equivalent DOM check, not by any test or lint rule, since none of the `renderToStaticMarkup`-only drawer tests could have caught missing runtime focus/keyboard behavior). Usage: `<Drawer onClose={...} labelledBy={titleId} widthClassName="w-[452px]">`, where the caller still owns and renders its own header/body/footer as `children` and puts `id={titleId}` (from its own `useId()`) on whichever element names the drawer — Drawer only owns the backdrop, panel scaffold, and the accessibility/focus-management behavior, not the drawer's internal layout.
- `governance` already exports `listProjectObjectives(db, actor, projectId)` (a project's own local objectives, no team/user mixing) and `listTeamObjectives`/`listUserObjectives`/`getObjective` — confirmed 2026-08-09 during `034-project-scoped-governance-ui`'s `/speckit-plan`, whose own research.md initially (wrongly) concluded no such function existed and planned to build one, because the barrel-scan during `/speckit-specify` only grepped for a few specific names rather than reading the full `governance/index.ts` export list. The duplicate was caught and deleted during `/speckit-implement`, not before — a full, unfiltered read of a BC's `index.ts` (not a targeted grep for names you already expect) is the only way to reliably confirm "no existing function does X" before planning to build it. `createObjective`/`updateObjective`/`deleteObjective` already fully support project scope too (including project-scope authorization, org-admin-only, no team-owner override — see `authorize-objective-action.ts`'s own comment flagging this as "revisit once a real project-objective feature exists").
- `src/app/api/projects/[projectId]/objectives/route.ts` and `src/app/api/objectives/[objectiveId]/route.ts` are a full existing REST CRUD surface for objectives (including project scope) — each has its own small `ObjectiveScopeVerifier`/`projectBelongsToOrganization` adapter calling `prompt-registry`'s `getProject` (checks a non-null return, not try/catch, since `getProject` returns `null` rather than throwing). Any new UI-layer (`"use server"` actions) caller needing the same adapter should mirror this REST-layer version's shape exactly rather than re-deriving it — same "each layer gets its own small copy, not a shared cross-route-group import" convention `project-identity-verifier.ts` already established.
- A list row needing both "click anywhere to edit" and a separate delete control must **not** nest a real `<button>`/`role="button"` inside the row's own `<button>` — invalid HTML and an axe violation (interactive-in-interactive). Correct pattern: a plain `<div>` row wrapping two sibling elements — a `<button>` around just the clickable content (e.g. title/description) for edit, and a separate `<button>` for delete — never one element playing both roles. Caught via axe testing (not by hand) while building `034-project-scoped-governance-ui`'s objective rows.
- Confirmed again 2026-08-09 (`034-project-scoped-governance-ui`): a commit unrelated to the active feature (a plain backlog reorganization, real user commit, correct author/timestamp) can land directly on whatever branch happens to be checked out in this shared working directory from a concurrent process — surfaced only via `git reflog`/`git log` after an unexpected `git push` diff. Don't try to surgically strip it out via rebase/reset (real, intentional work, risk of loss); leave it in place and disclose it plainly in the PR description instead. See `feedback_concurrent_session_edits` memory for the general pattern this reconfirms.

- The web UI for a chain-kind skill (`006-prompt-registry/archive/010-skill-chain-views-ui.md`) has **zero control that starts or advances a chain run**, by deliberate, already-shipped design — Prompt Registry never executes a step or observes a model's output, so a real run only ever happens client-side via direct REST calls (`POST /api/skills/[name]/chain-runs`, then `POST /api/chain-runs/[runId]/advance` per step) from whatever agent is actually executing the chain. Don't plan or spec a UI smoke test that assumes a "run" button exists; the UI's only job is the read-only Run History view. `035-web-ui-final-audit` caught its own spec making this wrong assumption during planning and corrected it before implementation.
- This self-hosted instance enforces a **single organization per deployment** (`createOrganization`'s guard, gated off unless `STRIPE_ENABLED`) — visiting `/register` while already signed in just redirects to `/dashboard`, it does not offer to create a second org. A live smoke test needing "a fresh org" on the shared dev stack must instead reuse the existing one (org "Acme Corp", team "Platform", user Alice Admin, established by earlier sessions' own smoke tests) — there is no way to get a second, isolated org on this stack short of a fresh database.
- A fully-completed epic (every feature archived) gets moved wholesale via `git mv backlog/<epic> backlog/done/<epic>` — not left in place at `backlog/<epic>/` with just its features individually archived. This is a real, already-established convention (observed via a concurrent session's commit moving `005-governance` and `006-prompt-registry` this way, mid-session, 2026-08-09/10) distinct from the existing per-feature `backlog/<epic>/archive/<item>.md` pattern — the whole-epic move happens once, when the epic's own `EPIC.md` Features list is 100% checked off. Cross-references to the old `backlog/<epic>/...` path in other epics' `Dependencies` sections and in historical `specs/*` files are **not** rewritten when this happens (confirmed: the precedent commit was a pure rename, 0 content changes) — leave them as dead-but-historical, don't chase them down.
- A distinct, simpler cause of the already-documented "shared dev DB behind on migrations" failure class (see the several entries above on stale volumes/`0024`): nobody re-ran `pnpm db:migrate` after pulling code that added new migrations — no volume/role issue at all, `docker exec spechub-database-1 psql ... "select count(*) from drizzle.__drizzle_migrations"` is just lower than `drizzle/migrations/meta/_journal.json`'s entry count. Symptom looks like an app bug (a live page 500s with `relation "..." does not exist`, or a list silently returns empty) but isn't one — confirmed 2026-08-10 (`035-web-ui-final-audit`), the shared stack was 3 migrations behind, breaking every prompt detail page and the Projects list simultaneously. Fix is the same non-destructive `pnpm db:migrate` re-run, no volume recreation needed.

- **A leftover Chrome service worker from a completely unrelated local project can silently intercept every request to `localhost:3000`**, including for this repo, serving stale cached documents even across brand-new tabs, hard reloads, and fresh logins — indistinguishable from a real "the app isn't updating" bug (confirmed 2026-08-10: a "cockpit" project's service worker with caches `cockpit-assets-v1`/`cockpit-api-v1`/`cockpit-runtime-v1`, scope `http://localhost:3000/`, was serving a multi-day-old cached `/prompts` page after a real rebuild+redeploy). Diagnose with `navigator.serviceWorker.getRegistrations()` / `caches.keys()` from a `javascript_tool` call on the affected tab — a non-empty result whose scope matches `localhost:3000` but whose cache names don't match this app is the smoking gun. Fix: `(await navigator.serviceWorker.getRegistrations()).forEach(r => r.unregister())` plus `(await caches.keys()).forEach(k => caches.delete(k))`, then reload. This is a browser-profile-level issue, not a server-side one — `docker compose up -d --build app` was already correct and already serving fresh content the whole time.
- When a `/speckit-specify` (or similar) session starts fresh mid-conversation with no established branch context, **check `git branch --show-current` before starting implementation work, not just before `/as-finish`** — this repo's shared local checkout can be sitting on an unrelated, already-merged prior epic's branch (confirmed 2026-08-10: a full `036-external-skill-import` implementation happened entirely on `035-web-ui-final-audit`, a closed epic whose own PR was already merged into `main`, before being caught at finish time). Recovering after the fact requires real git surgery — `git stash push -u -- <explicit list of this session's own files>` (never a bare `git stash -u`, which would also sweep up any pre-existing unrelated uncommitted changes already sitting in the working tree), `git checkout -b <feature-branch> origin/main`, `git stash pop` (expect a trivial conflict in `.specify/feature.json` against whatever feature the base branch last pointed at — just keep the current session's value) — avoidable entirely by branching correctly up front.
- The New Skill drawer's "Import from link" mode (013-skill-import-and-external-registries, `036-external-skill-import`) shows a CLI hint (`npx skills add <source>`) copied verbatim from the source Claude Design mockup — **this does not correspond to a real command**. The actual installed CLI package's bin name is `skillcanon`, not `skills` (`cli/package.json`), and it has no `add` subcommand at all (only `init`/`run`/`sync`, per `cli/src/commands/`). The backend fetch/parse/import pipeline and the web UI path are real and verified end-to-end against a live public repo; only the CLI-side `npx skills add`/`skillcanon add` entry point itself remains unbuilt — tracked in `backlog/013-skill-import-and-external-registries/001-external-skill-registry-import.md`, left `status: open`. A general lesson: a design mockup's literal command-line/UI copy can be aspirational rather than a statement of an already-real interface — verify against the actual package (`bin` field, `commands/` directory) before assuming a shown CLI invocation works.
- Unauthenticated calls to `api.github.com` are capped at 60 requests/hour per IP — a single multi-skill source fetch (one directory listing + one file fetch per file per skill) can burn through a large fraction of that budget in one `fetchExternalSkillSource` call (confirmed: scanning `anthropics/skills`, 16 skills, used enough of the hourly budget that a second unrelated attempt shortly after hit `ExternalSourceUnreachableError`/403). `getGithubToken()` (`src/shared/config`) already supports raising this to 5000/hour via a `GITHUB_TOKEN` env var (no scopes needed, since only public content is read) — it just isn't set anywhere by default. Set it in `.env.local` (for `pnpm dev`) and/or as a `docker-compose.yaml` pass-through (matching the existing `JWT_SECRET`/`SMTP_PASS` pattern) if doing repeated live testing of this feature.
- To browser-verify a UI change on the shared local dev stack when the actual login password for the seeded `alice@example.com` account is unknown (to the session or the user), **reset it directly**: hash a new password with `bcryptjs` (already a project dependency — `node -e "require('bcryptjs').hash('...', 10).then(console.log)"` run from the repo root, not a separate scratch script) and `UPDATE identity_access.users SET password_hash = '<hash>' WHERE email = 'alice@example.com'` via `docker compose exec -T database psql`, then log in through the real `/login` form. This is legitimate (it exercises the app's actual auth flow) and distinct from forging a session JWT directly, which the environment's own safety classifier correctly blocks as an auth-bypass technique — don't attempt that workaround. (The seeded `alice@example.com` account's actual password is `password` — try that first before reaching for a reset.)
- **A Server Action that internally calls `withTenantContext(db, ...)` with the module-level `db` singleton (the established pattern in `src/app/(app)/prompts/actions.ts` and every sibling actions file) cannot be redirected to a Testcontainers test database, even once any `next/headers()` dependency is solved separately.** `037-local-folder-skill-upload`'s `importLocalSkillsAction` needed a real Testcontainers test for its per-skill create/publish/error-isolation loop (the mechanism behind partial-batch-success on a name collision) — the fix was two separate, composable extractions: (1) pull the loop out into a separately-exported core function (`runLocalSkillImportBatch`) taking the already-resolved actor as a plain parameter instead of calling `requireActingUser()`/`headers()` itself, since `headers()` throws outside a real Next.js request context; (2) give that same function an optional `dbOverride: typeof db = db` parameter, defaulting to the real singleton in production but overridable to `testDb.appDb` in a test — mirrors `withApiRoute`'s existing `deps.db` override pattern for REST routes (`src/shared/api/handler.ts`), just applied to a Server Action instead of a route handler. Without step (2) alone, a test calling the extracted function still silently tries to hit whatever `DATABASE_URL` is configured for the real singleton, not the test container — confirmed by an initial attempt failing with empty `imported`/`failed` arrays (no thrown error, `withTenantContext` just executed against the wrong, likely-unreachable database). This same fix is very likely needed for `importExternalSkillsAction`'s equivalent, still-untested loop from `036-external-skill-import` if it's ever given the same Testcontainers treatment.
- `eslint-plugin-boundaries` blocks a test file living **outside** a bounded context (e.g. `src/app/(app)/prompts/*.test.ts`) from importing that BC's internal, non-barrel-exported modules — including a same-BC-internal test helper like `prompt-registry/application/prompt-test-helpers.ts`'s `makePromptFixtureOrg`, which every in-BC test file imports freely via a relative path. A composition-root-level test needing an org/team/user fixture (e.g. `actions.local-import.test.ts`, testing a Server Action's extracted core function) must build its own minimal fixture with raw SQL via `testDb.ownerDb` instead of importing the BC's fixture helper — same "own fixture, don't reach into another module's internals" resolution as this file's existing cross-BC audit-read note above, just triggered by directory location (inside `src/app/`) rather than by crossing a BC boundary.
- `webkitdirectory` (the real, widely-supported-if-non-standard HTML attribute that turns a plain `<input type="file">` into a folder picker) isn't in React's `InputHTMLAttributes` DOM typings — `pnpm typecheck` rejects it as an unknown JSX prop. Fix with a narrow, local TypeScript declaration-merging augmentation (`declare module "react" { interface InputHTMLAttributes<T> { webkitdirectory?: string } }`, scoped to the one file that renders the input — `037-local-folder-skill-upload`'s `new-prompt-drawer.tsx`) rather than an `as any`/`@ts-expect-error` escape hatch. Reuse this same augmentation for any future folder-picker UI rather than re-deriving it.
- The New Skill drawer (`src/app/(app)/prompts/new-prompt-drawer.tsx`) still has the exact conditionally-mounted-tab-panel testability gap this file already documents generically above ("A React tab component whose panels are conditionally unmounted... is untestable via this repo's `renderToStaticMarkup`-only testing convention for any tab besides the default") — it was never fixed when first written (`036-external-skill-import`'s "Import from link" panel body has zero structural test coverage of its own, only the tab-bar labels are asserted), and `037-local-folder-skill-upload`'s new "Import from folder" mode inherits the same gap rather than fixing it, to avoid unrelated scope creep on a shared file. A future session that needs to unit-test any of this drawer's dynamic post-fetch/post-scan content (the "import" mode's results list, the "upload" mode's preview list, either mode's failure states) will need to apply the documented fix (always-rendered-but-hidden panels) to this file first — until then, that content is verified only by live manual browser testing (see `specs/037-local-folder-skill-upload/quickstart.md`).

- `(app)/_components/app-shell.tsx`'s content column (`<div className="min-w-0">{children}</div>`) is a **CSS Grid item** (the shell root is `grid grid-cols-[216px_minmax(0,1fr)]`), not a flex container — a page's top-level wrapper must size itself with `h-full` (grid stretch already gives that div a concrete height from the row track), not `min-h-0 flex-1`, which is a silent no-op with no flex ancestor to size against. `prompts-list-view.tsx`/`projects-list-view.tsx` already used the correct `flex h-full flex-col` pattern; `governance-view.tsx` had drifted to the flex-only classes and, while it happened to look identical for short content (the shell's background is uniform, so a shrink-to-fit container is visually indistinguishable from a full-height one until content needs to scroll), fixed 2026-08-15 to match the established pattern.
- A fresh `.herdr/worktrees/SpecHub/worktree-*` checkout has **no `node_modules` at all** — running a bare `pnpm exec <tool>` (e.g. `tsc --noEmit`) silently no-ops with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "<tool>" not found` and, if piped through `grep`, produces empty output that looks identical to "no errors found." Run `pnpm install` first (fast — shared pnpm store, a few seconds) before trusting any `pnpm exec`-based typecheck/lint/test result in a new worktree.
- For a quick live-browser check of a UI-only change without waiting on the shared `spechub-app-1`/`spechub-database-1` Docker rebuild (confirmed slow again 2026-08-15), write a throwaway `.env.local` pointing `DATABASE_URL`/`MIGRATION_DATABASE_URL`/`AUTH_DATABASE_URL`/`JWT_SECRET` at the same already-running Compose Postgres (`docker-compose.yaml`'s `skillcanon_app`/`skillcanon`/`skillcanon_auth` role defaults, host `localhost:5432` since Compose already publishes that port) and run `pnpm exec next dev -p <free-port>` — both processes share the same database, and Next's dev server starts in under a second versus a multi-minute Docker image rebuild. `.env.local` is already gitignored (`.gitignore`'s `.env*` / `!.env.example` pair), so this is safe to leave in a worktree.

<!-- as-retro will add to this over time. -->
