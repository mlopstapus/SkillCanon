# Contracts

This feature adds no new REST/API contracts. It audits existing contracts (`src/app/api/**/route.ts`) for wiring into the composed UI — see `../parity-audit.md` and `../quickstart.md` for the audit results.
